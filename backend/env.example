# ===========================================
# BASE DE DONNÉES
# ===========================================

# PostgreSQL (Production - Neon, Supabase, etc.)
DATABASE_URL="postgresql://username:password@localhost:5432/lahamarchand_benin"

# SQLite (Développement local - décommentez pour utiliser)
# DATABASE_URL="file:./dev.db"

# ===========================================
# NEXTAUTH - AUTHENTIFICATION
# ===========================================

# URL de l'application
NEXTAUTH_URL="http://localhost:3000"

# Secret pour NextAuth (générez avec: openssl rand -base64 32)
NEXTAUTH_SECRET="your-super-secret-key-change-this-in-production"

# ===========================================
# SÉCURITÉ
# ===========================================

# Clé secrète pour créer des comptes PDG via API
PDG_CREATION_SECRET="change-this-secret-key-in-production"

# ===========================================
# ENVIRONNEMENT
# ===========================================

NODE_ENV="development"

# Configuration Email (optionnel)
EMAIL_SERVER_HOST="smtp.gmail.com"
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER="your-email@gmail.com"
EMAIL_SERVER_PASSWORD="your-app-password"
EMAIL_FROM="noreply@lahamarchand-benin.com"

# Configuration Paiement (optionnel)
STRIPE_PUBLIC_KEY="pk_test_..."
STRIPE_SECRET_KEY="sk_test_..."

# Configuration Upload (optionnel)
CLOUDINARY_CLOUD_NAME="your-cloud-name"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"
